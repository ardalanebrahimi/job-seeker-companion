openapi: 3.1.0
info:
  title: Job Companion API
  version: 1.0.0
  description: |
    OpenAPI-first contract for Job Companion (V0–V1).
    Source of truth for SDK generation and backend implementation.

servers:
  - url: https://api.example.com/v1
    description: Production
  - url: http://localhost:8080/v1
    description: Local

security:
  - bearerAuth: []

tags:
  - name: Users
  - name: Jobs
  - name: Applications
  - name: Coach
  - name: Research

paths:
  /users/me/cv:
    post:
      tags: [Users]
      summary: Upload base CV (DOCX or PDF)
      operationId: uploadBaseCv
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [file]
              properties:
                file:
                  type: string
                  format: binary
                filename:
                  type: string
                language:
                  type: string
                  description: ISO language tag (e.g., en, de)
      responses:
        "201":
          description: CV stored and parsed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CvUploadResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
    get:
      tags: [Users]
      summary: Get parsed base CV preview
      operationId: getBaseCvPreview
      responses:
        "200":
          description: Parsed CV facts and file references
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CvPreview"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /jobs/ingest:
    post:
      tags: [Jobs]
      summary: Ingest a job from URL or raw text
      operationId: ingestJob
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JobIngestRequest"
      responses:
        "201":
          description: Job ingested
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobIngestResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /jobs/{id}:
    get:
      tags: [Jobs]
      summary: Get job details and basic analysis
      operationId: getJob
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Job with JD text and extracted structure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/JobDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /jobs/check-duplicate:
    post:
      tags: [Jobs]
      summary: Check if a job is a duplicate of an existing one (V2)
      operationId: checkJobDuplicate
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/JobIngestRequest"
      responses:
        "200":
          description: Duplicate check result
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DuplicateCheckResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /applications:
    get:
      tags: [Applications]
      summary: List applications with filtering and search (V2)
      operationId: listApplications
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: pageSize
          in: query
          schema:
            type: integer
            default: 20
            maximum: 100
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ApplicationStatus"
        - name: company
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
          description: Search in company, title, and notes
        - name: sortBy
          in: query
          schema:
            type: string
            enum: [createdAt, appliedAt, status, company]
            default: createdAt
        - name: sortOrder
          in: query
          schema:
            type: string
            enum: [asc, desc]
            default: desc
      responses:
        "200":
          description: List of applications
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationList"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /applications/generate:
    post:
      tags: [Applications]
      summary: Generate tailored CV & cover letter for a job (auto persona + RI)
      operationId: generateApplicationDocuments
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ApplicationGenerateRequest"
      responses:
        "201":
          description: Application created with generated documents
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationGenerateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/export:
    get:
      tags: [Applications]
      summary: Export applications list (V2)
      operationId: exportApplications
      parameters:
        - name: format
          in: query
          schema:
            type: string
            enum: [csv, json]
            default: csv
        - name: status
          in: query
          schema:
            $ref: "#/components/schemas/ApplicationStatus"
        - name: company
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
      responses:
        "200":
          description: Exported applications data
          content:
            text/csv:
              schema:
                type: string
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ApplicationSummary"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /applications/{id}:
    get:
      tags: [Applications]
      summary: Get application detail (docs + decisions)
      operationId: getApplicationDetail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Application detail
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationDetail"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/variants:
    post:
      tags: [Applications]
      summary: Generate multiple document variants for an application (V3)
      operationId: generateDocumentVariants
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VariantGenerateRequest"
      responses:
        "201":
          description: Document variants generated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VariantGenerateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/documents/{documentId}/preview:
    get:
      tags: [Applications]
      summary: Preview a document in the browser (V3)
      operationId: previewDocument
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Document preview
          content:
            text/html:
              schema:
                type: string
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/documents/{documentId}/download:
    get:
      tags: [Applications]
      summary: Download a document file (V3)
      operationId: downloadDocument
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Document file
          content:
            application/vnd.openxmlformats-officedocument.wordprocessingml.document:
              schema:
                type: string
                format: binary
            application/pdf:
              schema:
                type: string
                format: binary
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/documents/{documentId}/diff:
    get:
      tags: [Applications]
      summary: Get diff between generated document and base CV (V3)
      operationId: getDocumentDiff
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
        - name: documentId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Document diff
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentDiff"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/documents/history:
    get:
      tags: [Applications]
      summary: Get document history with versions (V3)
      operationId: getDocumentHistory
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Document history
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/DocumentHistory"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/notes:
    post:
      tags: [Applications]
      summary: Add a note to an application
      operationId: addApplicationNote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoteCreate"
      responses:
        "201":
          description: Note stored
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/notes/{noteId}:
    put:
      tags: [Applications]
      summary: Update a note on an application (V2)
      operationId: updateApplicationNote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
        - name: noteId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/NoteCreate"
      responses:
        "200":
          description: Note updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Note"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    delete:
      tags: [Applications]
      summary: Delete a note from an application (V2)
      operationId: deleteApplicationNote
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
        - name: noteId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: Note deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/reminders:
    post:
      tags: [Applications]
      summary: Set a reminder for an application (V2)
      operationId: setApplicationReminder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ReminderCreate"
      responses:
        "201":
          description: Reminder set
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reminder"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    get:
      tags: [Applications]
      summary: Get reminders for an application (V2)
      operationId: getApplicationReminders
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Application reminders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/Reminder"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/delete:
    delete:
      tags: [Applications]
      summary: Delete an application and all linked documents (V2)
      operationId: deleteApplication
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "204":
          description: Application deleted
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/regenerate:
    post:
      tags: [Applications]
      summary: Regenerate application docs with modified switches/settings (V1)
      operationId: regenerateApplicationDocuments
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                switches:
                  type: array
                  description: Modified emphasis switches
                  items:
                    type: object
                    properties:
                      label: { type: string }
                      active: { type: boolean }
                realityIndex:
                  type: integer
                  minimum: 0
                  maximum: 2
                stylePreference:
                  type: string
                  enum: [concise, balanced, detailed]
      responses:
        "200":
          description: Application regenerated with new settings
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationGenerateResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/status:
    post:
      tags: [Applications]
      summary: Update application status
      operationId: updateApplicationStatus
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/StatusUpdate"
      responses:
        "200":
          description: Status updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ApplicationSummary"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /reminders:
    get:
      tags: [Applications]
      summary: Get upcoming reminders across all applications (V2)
      operationId: getUpcomingReminders
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
            maximum: 50
      responses:
        "200":
          description: Upcoming reminders
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ReminderWithApplication"
        "401":
          $ref: "#/components/responses/Unauthorized"

  /reminders/{id}/complete:
    post:
      tags: [Applications]
      summary: Mark a reminder as completed (V2)
      operationId: completeReminder
      parameters:
        - name: id
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Reminder marked as completed
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Reminder"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /coach/nudges:
    post:
      tags: [Coach]
      summary: Get coaching nudges for a specific job application (V1)
      operationId: getCoachingNudges
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [jobId]
              properties:
                jobId:
                  type: string
                  format: uuid
                applicationId:
                  type: string
                  format: uuid
                  description: Optional - for application-specific advice
      responses:
        "200":
          description: Coaching nudges and actionable tips
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoachingNudges"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /coach/hints/{applicationId}:
    get:
      tags: [Coach]
      summary: Get coaching hints for an application (V2)
      operationId: getApplicationCoachingHints
      parameters:
        - name: applicationId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      responses:
        "200":
          description: Coaching hints for this application
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoachingHints"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /coach/hints/{applicationId}/dismiss:
    post:
      tags: [Coach]
      summary: Dismiss a coaching hint for an application (V2)
      operationId: dismissCoachingHint
      parameters:
        - name: applicationId
          in: path
          required: true
          schema:
            format: uuid
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [hintId]
              properties:
                hintId:
                  type: string
      responses:
        "204":
          description: Hint dismissed
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /coach/gap-roadmap:
    post:
      tags: [Coach]
      summary: Generate gap analysis and short roadmap for a job
      operationId: generateGapRoadmap
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [jobId]
              properties:
                jobId:
                  type: string
                  format: uuid
                personaHint:
                  type: string
                  description: Optional persona label override
      responses:
        "200":
          description: Gap analysis and roadmap
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/GapRoadmap"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  # V4 Company Brochure & Interview Hub
  /companies/{id}/snapshot:
    get:
      tags: [Research]
      summary: Get company snapshot (basic card)
      operationId: getCompanySnapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Company snapshot
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanySnapshot"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /companies/{id}/brochure:
    get:
      tags: [Research]
      summary: Get multimedia company brochure
      operationId: getCompanyBrochure
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Company brochure
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanyBrochure"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/company/snapshot:
    get:
      tags: [Applications]
      summary: Get company snapshot for application
      operationId: getApplicationCompanySnapshot
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Company snapshot for this application
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CompanySnapshot"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/fit-brief:
    get:
      tags: [Applications]
      summary: Get role-specific fit brief
      operationId: getRoleSpecificFitBrief
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Role-specific fit brief
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RoleSpecificFitBrief"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/interview-prep:
    get:
      tags: [Applications]
      summary: Get interview prep pack
      operationId: getInterviewPrepPack
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Interview prep pack
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewPrepPack"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/star-stories:
    get:
      tags: [Applications]
      summary: Generate STAR stories for behavioral questions
      operationId: generateStarStories
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Generated STAR stories
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/StarStoriesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/likely-questions:
    get:
      tags: [Applications]
      summary: Get likely interview questions
      operationId: getLikelyQuestions
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Likely interview questions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LikelyQuestionsResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/interview-notes:
    get:
      tags: [Applications]
      summary: Get interview notes for application
      operationId: getInterviewNotes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Interview notes
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewNotesResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Applications]
      summary: Add interview notes
      operationId: addInterviewNotes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InterviewNotesCreate"
      responses:
        "201":
          description: Interview notes added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewNotes"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/interview-notes/{noteId}:
    put:
      tags: [Applications]
      summary: Update interview notes
      operationId: updateInterviewNotes
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: noteId
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InterviewNotesUpdate"
      responses:
        "200":
          description: Interview notes updated
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewNotes"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/feedback:
    get:
      tags: [Applications]
      summary: Get interview feedback log
      operationId: getInterviewFeedback
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Interview feedback log
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewFeedbackResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"
    post:
      tags: [Applications]
      summary: Add interview feedback
      operationId: addInterviewFeedback
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/InterviewFeedbackCreate"
      responses:
        "201":
          description: Interview feedback added
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/InterviewFeedback"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/thank-you-email:
    post:
      tags: [Applications]
      summary: Generate thank-you email after interview
      operationId: generateThankYouEmail
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ThankYouEmailRequest"
      responses:
        "200":
          description: Generated thank-you email
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ThankYouEmailResponse"
        "400":
          $ref: "#/components/responses/BadRequest"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

  /applications/{id}/coaching-companion:
    get:
      tags: [Applications]
      summary: Get coaching companion suggestions for interview hub
      operationId: getCoachingCompanion
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Coaching companion suggestions
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CoachingCompanionResponse"
        "401":
          $ref: "#/components/responses/Unauthorized"
        "404":
          $ref: "#/components/responses/NotFound"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    BadRequest:
      description: Bad request
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    Unauthorized:
      description: Unauthorized
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFound:
      description: Not found
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"

  schemas:
    Error:
      type: object
      properties:
        error:
          type: object
          properties:
            code: { type: string }
            message: { type: string }
          required: [code, message]
      required: [error]

    # --- Users / CV ---
    CvUploadResponse:
      type: object
      properties:
        fileId:
          type: string
          format: uuid
        parsed:
          type: boolean
        language:
          type: string
        preview:
          $ref: "#/components/schemas/CvPreview"
      required: [fileId, parsed]

    CvPreview:
      type: object
      properties:
        summary:
          type: string
        skills:
          type: array
          items: { type: string }
        experience:
          type: array
          items:
            type: object
            properties:
              company: { type: string }
              title: { type: string }
              startDate: { type: string, format: date }
              endDate: { type: string, format: date, nullable: true }
              bullets:
                type: array
                items: { type: string }
        education:
          type: array
          items:
            type: object
            properties:
              institution: { type: string }
              degree: { type: string }
              year: { type: string }
        fileUri:
          type: string

    # --- Jobs ---
    JobIngestRequest:
      type: object
      properties:
        url:
          type: string
          format: uri
        rawText:
          type: string
        language:
          type: string
          description: Optional hint (e.g., en, de)
      oneOf:
        - required: [url]
        - required: [rawText]

    JobIngestResponse:
      type: object
      properties:
        jobId:
          type: string
          format: uuid
      required: [jobId]

    JobDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company:
          type: string
        title:
          type: string
        location:
          type: string
        jdText:
          type: string
        jdStruct:
          $ref: "#/components/schemas/JdStruct"
        firstSeenAt:
          type: string
          format: date-time
      required: [id, jdText]

    JdStruct:
      type: object
      properties:
        skills:
          type: array
          items: { type: string }
        requirements:
          type: array
          items: { type: string }
        niceToHave:
          type: array
          items: { type: string }
        seniority:
          type: string

    # --- Applications ---
    ApplicationGenerateRequest:
      type: object
      required: [jobId]
      properties:
        jobId:
          type: string
          format: uuid
        realityIndex:
          type: integer
          minimum: 0
          maximum: 2
          description: Optional override; default decided by agent (V1)
        language:
          type: string
          description: Output language; defaults to JD language
        personaHint:
          type: string
          description: Optional persona override; agent auto-decides if not provided (V1)
        stylePreference:
          type: string
          enum: [concise, balanced, detailed]
          description: Optional style preference; agent auto-decides if not provided (V1)

    ApplicationGenerateResponse:
      type: object
      properties:
        applicationId:
          type: string
          format: uuid
        docs:
          type: array
          items:
            $ref: "#/components/schemas/GeneratedDoc"
        decision:
          $ref: "#/components/schemas/Decision"
      required: [applicationId, docs]

    GeneratedDoc:
      type: object
      properties:
        kind:
          $ref: "#/components/schemas/DocumentKind"
        format:
          $ref: "#/components/schemas/DocumentFormat"
        uri:
          type: string
        variantLabel:
          type: string
          description: e.g., concise | balanced | detailed
        language:
          type: string
      required: [kind, format, uri]

    ApplicationList:
      type: object
      properties:
        items:
          type: array
          items:
            $ref: "#/components/schemas/ApplicationSummary"
        page:
          type: integer
        pageSize:
          type: integer
        total:
          type: integer
      required: [items, page, pageSize, total]

    ApplicationSummary:
      type: object
      properties:
        id:
          type: string
          format: uuid
        company:
          type: string
        title:
          type: string
        status:
          $ref: "#/components/schemas/ApplicationStatus"
        createdAt:
          type: string
          format: date-time
        appliedAt:
          type: string
          format: date-time
          nullable: true
        nextReminderAt:
          type: string
          format: date-time
          nullable: true
          description: Next upcoming reminder date (V2)
        hasNotes:
          type: boolean
          description: Whether this application has notes (V2)
        coachingHint:
          type: string
          nullable: true
          description: Primary coaching hint for tracker view (V2)
        location:
          type: string
          nullable: true
          description: Job location (V2)
      required: [id, status, createdAt, hasNotes]

    ApplicationDetail:
      type: object
      properties:
        id:
          type: string
          format: uuid
        job:
          $ref: "#/components/schemas/JobDetail"
        status:
          $ref: "#/components/schemas/ApplicationStatus"
        notes:
          type: array
          items:
            $ref: "#/components/schemas/Note"
        docs:
          type: array
          items:
            $ref: "#/components/schemas/GeneratedDoc"
        decision:
          $ref: "#/components/schemas/Decision"
        reminders:
          type: array
          items:
            $ref: "#/components/schemas/Reminder"
          description: Active reminders for this application (V2)
        history:
          $ref: "#/components/schemas/ApplicationHistory"
          description: History log of all changes (V2)
        coachingHints:
          $ref: "#/components/schemas/CoachingHints"
          description: Current coaching hints (V2)
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, status, createdAt]

    Decision:
      type: object
      properties:
        persona:
          type: string
          description: Auto-decided persona label (V1)
        realityIndex:
          type: integer
          description: Applied Reality Index (0-2) (V1)
        signals:
          type: array
          items: { type: string }
          description: Top signals that influenced persona choice (V1)
        keywordsEmphasized:
          type: array
          items: { type: string }
          description: JD keywords emphasized in output (V1)
        styleRationale:
          type: string
          description: Why this style/template was chosen (V1)
        switches:
          type: array
          description: Available emphasis toggles for re-generation (V1)
          items:
            type: object
            properties:
              label:
                type: string
                description: e.g., "Emphasize leadership"
              active:
                type: boolean
                description: Current state of this switch
        provenanceLinks:
          type: array
          description: Claim-to-fact mappings for transparency (V1)
          items:
            type: object
            properties:
              claimText:
                type: string
                description: Text of the claim in generated document
              sourceFactId:
                type: string
                description: ID of supporting resume fact
              factType:
                type: string
                enum: [role, project, education, cert, skill]

    NoteCreate:
      type: object
      required: [text]
      properties:
        text:
          type: string

    Note:
      type: object
      properties:
        id:
          type: string
          format: uuid
        text:
          type: string
        createdAt:
          type: string
          format: date-time
      required: [id, text, createdAt]

    StatusUpdate:
      type: object
      required: [status]
      properties:
        status:
          $ref: "#/components/schemas/ApplicationStatus"

    ApplicationStatus:
      type: string
      enum: [Found, Applied, Interview, Offer, Rejected]

    DocumentKind:
      type: string
      enum: [cv, cover, brochure, prep]

    DocumentFormat:
      type: string
      enum: [md, docx, pdf]

    # --- Coach ---
    CoachingNudges:
      type: object
      properties:
        nudges:
          type: array
          description: 3-5 actionable coaching tips specific to this JD (V1)
          items:
            type: object
            properties:
              category:
                type: string
                enum: [emphasize, trim, gap, improvement]
              title:
                type: string
              description:
                type: string
              priority:
                type: string
                enum: [high, medium, low]
        immediateActions:
          type: array
          description: Things to address this week
          items:
            type: object
            properties:
              action: { type: string }
              etaDays: { type: integer }
        jdThemes:
          type: array
          description: Key themes from JD that advice references
          items: { type: string }

    GapRoadmap:
      type: object
      properties:
        matchScore:
          type: number
          format: float
        mustHaveGaps:
          type: array
          items: { type: string }
        niceToHaveGaps:
          type: array
          items: { type: string }
        actions:
          type: array
          description: Actionable steps or micro-projects
          items:
            type: object
            properties:
              title: { type: string }
              description: { type: string }
              etaDays: { type: integer }

    # --- V2 Schemas ---
    DuplicateCheckResponse:
      type: object
      properties:
        isDuplicate:
          type: boolean
        similarityScore:
          type: number
          format: float
          description: Similarity score (0-1)
        existingJobId:
          type: string
          format: uuid
          nullable: true
        existingApplicationId:
          type: string
          format: uuid
          nullable: true
        reason:
          type: string
          description: Explanation of why it's considered a duplicate
        canOverride:
          type: boolean
          description: Whether user can override and save as new
      required: [isDuplicate, canOverride]

    ReminderCreate:
      type: object
      required: [dueAt, kind]
      properties:
        dueAt:
          type: string
          format: date-time
        kind:
          type: string
          enum: [followup, interview, task]
        title:
          type: string
        description:
          type: string

    Reminder:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        dueAt:
          type: string
          format: date-time
        kind:
          type: string
          enum: [followup, interview, task]
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
        completedAt:
          type: string
          format: date-time
          nullable: true
        createdAt:
          type: string
          format: date-time
      required: [id, applicationId, dueAt, kind, completed, createdAt]

    ReminderWithApplication:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        dueAt:
          type: string
          format: date-time
        kind:
          type: string
          enum: [followup, interview, task]
        title:
          type: string
        description:
          type: string
        completed:
          type: boolean
        application:
          type: object
          properties:
            id:
              type: string
              format: uuid
            company:
              type: string
            title:
              type: string
            status:
              $ref: "#/components/schemas/ApplicationStatus"
          required: [id, status]
      required: [id, applicationId, dueAt, kind, completed, application]

    CoachingHints:
      type: object
      properties:
        hints:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
              category:
                type: string
                enum: [next_step, follow_up, improvement, preparation]
              title:
                type: string
              description:
                type: string
              priority:
                type: string
                enum: [high, medium, low]
              dismissed:
                type: boolean
            required: [id, category, title, description, priority, dismissed]
        nextStepHint:
          type: string
          nullable: true
          description: Primary next step recommendation
      required: [hints]

    ApplicationHistory:
      type: object
      properties:
        entries:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              timestamp:
                type: string
                format: date-time
              type:
                type: string
                enum:
                  [
                    status_change,
                    note_added,
                    note_updated,
                    note_deleted,
                    reminder_set,
                    reminder_completed,
                    document_generated,
                  ]
              description:
                type: string
              metadata:
                type: object
                additionalProperties: true
            required: [id, timestamp, type, description]
      required: [entries]

    # --- V3 Schemas ---
    VariantGenerateRequest:
      type: object
      properties:
        variants:
          type: array
          items:
            type: string
            enum: [concise, balanced, detailed]
          default: [concise, detailed]
        regenerateExisting:
          type: boolean
          default: false
        targetFormat:
          $ref: "#/components/schemas/DocumentFormat"
          default: docx
      required: [variants]

    VariantGenerateResponse:
      type: object
      properties:
        variants:
          type: array
          items:
            type: object
            properties:
              variantLabel:
                type: string
                enum: [concise, balanced, detailed]
              documents:
                type: array
                items:
                  $ref: "#/components/schemas/GeneratedDoc"
              coachingNote:
                type: string
                description: Short advice on when to use this variant
            required: [variantLabel, documents]
        recommendedVariant:
          type: string
          enum: [concise, balanced, detailed]
          description: Agent's recommendation for this specific job
      required: [variants]

    DocumentDiff:
      type: object
      properties:
        documentId:
          type: string
          format: uuid
        changes:
          type: array
          items:
            type: object
            properties:
              type:
                type: string
                enum: [added, removed, modified]
              section:
                type: string
                description: Section name (e.g., "Experience", "Skills")
              originalText:
                type: string
                nullable: true
              modifiedText:
                type: string
                nullable: true
              reason:
                type: string
                description: Why this change was made
              sourceFact:
                type: string
                description: Reference to source fact in resume
            required: [type, section]
        summary:
          type: object
          properties:
            addedCount:
              type: integer
            removedCount:
              type: integer
            modifiedCount:
              type: integer
            keyChanges:
              type: array
              items:
                type: string
          required: [addedCount, removedCount, modifiedCount, keyChanges]
      required: [documentId, changes, summary]

    DocumentHistory:
      type: object
      properties:
        applicationId:
          type: string
          format: uuid
        documents:
          type: array
          items:
            type: object
            properties:
              id:
                type: string
                format: uuid
              kind:
                $ref: "#/components/schemas/DocumentKind"
              format:
                $ref: "#/components/schemas/DocumentFormat"
              variantLabel:
                type: string
                nullable: true
              version:
                type: integer
              uri:
                type: string
              createdAt:
                type: string
                format: date-time
              isCurrent:
                type: boolean
                description: Whether this is the current version for this variant
            required: [id, kind, format, version, uri, createdAt, isCurrent]
      required: [applicationId, documents]

    # Enhanced GeneratedDoc for V3
    EnhancedGeneratedDoc:
      allOf:
        - $ref: "#/components/schemas/GeneratedDoc"
        - type: object
          properties:
            id:
              type: string
              format: uuid
            version:
              type: integer
            createdAt:
              type: string
              format: date-time
            templateStyle:
              type: string
              enum: [modern, minimal, classic]
            coachingNote:
              type: string
              nullable: true
            previewUrl:
              type: string
              description: URL for browser preview
            downloadUrl:
              type: string
              description: URL for file download
          required: [id, version, createdAt, templateStyle]

    # V4 Schemas - Company Brochure & Interview Hub
    CompanySnapshot:
      type: object
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
          description: Company name
        logo:
          type: string
          description: URL to company logo
        website:
          type: string
          description: Company website URL
        industry:
          type: string
          description: Industry category
        size:
          type: string
          description: Company size (e.g., "50-200 employees")
        headquarters:
          type: string
          description: HQ location
        summary:
          type: string
          description: 2-3 sentence summary of products/services
        recentNews:
          type: array
          items:
            type: object
            properties:
              title:
                type: string
              url:
                type: string
              date:
                type: string
                format: date
              summary:
                type: string
            required: [title, date, summary]
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required: [id, name, industry, summary, createdAt, updatedAt]

    CompanyBrochure:
      type: object
      properties:
        id:
          type: string
          format: uuid
        companyId:
          type: string
          format: uuid
        snapshot:
          $ref: "#/components/schemas/CompanySnapshot"
        sections:
          type: object
          properties:
            products:
              type: object
              properties:
                title:
                  type: string
                content:
                  type: string
                images:
                  type: array
                  items:
                    type: string
                    description: Image URLs
            market:
              type: object
              properties:
                title:
                  type: string
                content:
                  type: string
                competitors:
                  type: array
                  items:
                    type: string
            culture:
              type: object
              properties:
                title:
                  type: string
                content:
                  type: string
                values:
                  type: array
                  items:
                    type: string
            timeline:
              type: array
              items:
                type: object
                properties:
                  year:
                    type: integer
                  event:
                    type: string
                  description:
                    type: string
                required: [year, event]
        exportUrl:
          type: string
          description: URL to export as PDF
        createdAt:
          type: string
          format: date-time
      required: [id, companyId, snapshot, sections, createdAt]

    RoleSpecificFitBrief:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        fitReasons:
          type: array
          items:
            type: object
            properties:
              reason:
                type: string
              evidence:
                type: string
              strength:
                type: string
                enum: [high, medium, low]
            required: [reason, evidence, strength]
        skillAlignments:
          type: array
          items:
            type: object
            properties:
              skill:
                type: string
              userExperience:
                type: string
              companyNeed:
                type: string
              matchScore:
                type: number
                minimum: 0
                maximum: 1
            required: [skill, userExperience, companyNeed, matchScore]
        tone:
          type: string
          default: "coaching"
        createdAt:
          type: string
          format: date-time
      required: [id, applicationId, fitReasons, skillAlignments, createdAt]

    InterviewPrepPack:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        companySnapshot:
          $ref: "#/components/schemas/CompanySnapshot"
        fitBrief:
          $ref: "#/components/schemas/RoleSpecificFitBrief"
        starStories:
          type: array
          items:
            $ref: "#/components/schemas/StarStory"
        likelyQuestions:
          type: array
          items:
            $ref: "#/components/schemas/InterviewQuestion"
        exportUrl:
          type: string
          description: URL to export prep pack as PDF
        createdAt:
          type: string
          format: date-time
      required:
        [
          id,
          applicationId,
          companySnapshot,
          fitBrief,
          starStories,
          likelyQuestions,
          createdAt,
        ]

    StarStory:
      type: object
      properties:
        id:
          type: string
          format: uuid
        title:
          type: string
          description: Brief title for the story
        category:
          type: string
          description: JD requirement this story addresses
        situation:
          type: string
          description: Situation description
        task:
          type: string
          description: Task that needed to be done
        action:
          type: string
          description: Actions taken
        result:
          type: string
          description: Results achieved
        realityIndex:
          type: number
          minimum: 0
          maximum: 2
          description: Reality index used for this story
        isEditable:
          type: boolean
          default: true
        createdAt:
          type: string
          format: date-time
      required:
        [
          id,
          title,
          category,
          situation,
          task,
          action,
          result,
          realityIndex,
          createdAt,
        ]

    StarStoriesResponse:
      type: object
      properties:
        stories:
          type: array
          items:
            $ref: "#/components/schemas/StarStory"
        total:
          type: integer
        applicationId:
          type: string
          format: uuid
      required: [stories, total, applicationId]

    InterviewQuestion:
      type: object
      properties:
        id:
          type: string
          format: uuid
        question:
          type: string
        category:
          type: string
          enum: [role-specific, company-specific, behavioral]
        difficulty:
          type: string
          enum: [easy, medium, hard]
        linkedStoryId:
          type: string
          format: uuid
          nullable: true
          description: ID of linked STAR story if applicable
        tips:
          type: array
          items:
            type: string
      required: [id, question, category, difficulty]

    LikelyQuestionsResponse:
      type: object
      properties:
        questions:
          type: array
          items:
            $ref: "#/components/schemas/InterviewQuestion"
        applicationId:
          type: string
          format: uuid
        generatedAt:
          type: string
          format: date-time
      required: [questions, applicationId, generatedAt]

    InterviewNotes:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        interviewDate:
          type: string
          format: date-time
        interviewerName:
          type: string
          nullable: true
        interviewType:
          type: string
          enum: [phone, video, in-person, panel]
        duration:
          type: integer
          description: Duration in minutes
          nullable: true
        notes:
          type: string
          description: Detailed interview notes
        rating:
          type: integer
          minimum: 1
          maximum: 5
          nullable: true
          description: Self-assessment rating
        nextSteps:
          type: string
          nullable: true
          description: Mentioned next steps
        createdAt:
          type: string
          format: date-time
        updatedAt:
          type: string
          format: date-time
      required:
        [
          id,
          applicationId,
          interviewDate,
          interviewType,
          notes,
          createdAt,
          updatedAt,
        ]

    InterviewNotesCreate:
      type: object
      properties:
        interviewDate:
          type: string
          format: date-time
        interviewerName:
          type: string
          nullable: true
        interviewType:
          type: string
          enum: [phone, video, in-person, panel]
        duration:
          type: integer
          description: Duration in minutes
          nullable: true
        notes:
          type: string
          description: Detailed interview notes
        rating:
          type: integer
          minimum: 1
          maximum: 5
          nullable: true
        nextSteps:
          type: string
          nullable: true
      required: [interviewDate, interviewType, notes]

    InterviewNotesUpdate:
      type: object
      properties:
        interviewDate:
          type: string
          format: date-time
        interviewerName:
          type: string
          nullable: true
        interviewType:
          type: string
          enum: [phone, video, in-person, panel]
        duration:
          type: integer
          description: Duration in minutes
          nullable: true
        notes:
          type: string
          description: Detailed interview notes
        rating:
          type: integer
          minimum: 1
          maximum: 5
          nullable: true
        nextSteps:
          type: string
          nullable: true

    InterviewNotesResponse:
      type: object
      properties:
        notes:
          type: array
          items:
            $ref: "#/components/schemas/InterviewNotes"
        applicationId:
          type: string
          format: uuid
        total:
          type: integer
      required: [notes, applicationId, total]

    InterviewFeedback:
      type: object
      properties:
        id:
          type: string
          format: uuid
        applicationId:
          type: string
          format: uuid
        interviewDate:
          type: string
          format: date-time
        feedbackSource:
          type: string
          description: Who provided the feedback
        feedback:
          type: string
          description: Detailed feedback
        areasForImprovement:
          type: array
          items:
            type: string
        positivePoints:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
      required: [id, applicationId, interviewDate, feedback, createdAt]

    InterviewFeedbackCreate:
      type: object
      properties:
        interviewDate:
          type: string
          format: date-time
        feedbackSource:
          type: string
        feedback:
          type: string
        areasForImprovement:
          type: array
          items:
            type: string
        positivePoints:
          type: array
          items:
            type: string
      required: [interviewDate, feedback]

    InterviewFeedbackResponse:
      type: object
      properties:
        feedback:
          type: array
          items:
            $ref: "#/components/schemas/InterviewFeedback"
        applicationId:
          type: string
          format: uuid
        total:
          type: integer
      required: [feedback, applicationId, total]

    ThankYouEmailRequest:
      type: object
      properties:
        interviewerName:
          type: string
          nullable: true
        interviewDate:
          type: string
          format: date-time
        interviewType:
          type: string
          enum: [phone, video, in-person, panel]
        keyDiscussionPoints:
          type: array
          items:
            type: string
        personalNote:
          type: string
          nullable: true
          description: Optional personal note to include
      required: [interviewDate, interviewType]

    ThankYouEmailResponse:
      type: object
      properties:
        subject:
          type: string
        body:
          type: string
        recipientName:
          type: string
          nullable: true
        generatedAt:
          type: string
          format: date-time
      required: [subject, body, generatedAt]

    CoachingCompanionResponse:
      type: object
      properties:
        motivationalAdvice:
          type: string
          description: Motivational advice for interview prep
        practiceSuggestion:
          type: string
          description: Specific practice suggestion
        feedbackBasedTips:
          type: array
          items:
            type: string
          description: Tips based on feedback logs
        starStoryGaps:
          type: array
          items:
            type: string
          description: Areas where more STAR stories are needed
        lastUpdated:
          type: string
          format: date-time
        applicationId:
          type: string
          format: uuid
      required:
        [
          motivationalAdvice,
          practiceSuggestion,
          feedbackBasedTips,
          starStoryGaps,
          lastUpdated,
          applicationId,
        ]
